<%= form_for(@pharmacology) do |f| %>
  <% if @pharmacology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pharmacology.errors.count, "error") %> prohibited this pharmacology from being saved:</h2>

      <ul>
      <% @pharmacology.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
